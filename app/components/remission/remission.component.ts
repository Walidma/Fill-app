import { Component } from '@angular/core';
import {Auth} from '../../services/auth.service';

@Component({
    moduleId: module.id,
    selector: 'remission',
    templateUrl: 'remission.component.html'
})
export class RemissionComponent { 
    
    constructor(){
        
    }
}
